import { test, expect } from '../../src/fixture';

test.describe('Shopping Page Smoke Tests @smoke', () => {
    test('Check if product catalog is displayed', async ({ page }) => {
        // Placeholder
    });
});
